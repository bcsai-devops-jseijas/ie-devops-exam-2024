{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "appServiceContainerBackendName": {
            "value": "{alias}-asc-backend" //Replace {alias} with your student alias to avoid deployment conflicts 
        },
        "appServicePlanName": {
            "value": "{alias}-asp" //Replace {alias} with your student alias to avoid deployment conflicts 
        },
        "containerRegistryName": {
            "value": "{alias}acr" //Replace {alias} with your student alias to avoid deployment conflicts 
        },
        "postgreSQLDatabaseName": {
            "value": "{alias}-db" //Replace {alias} with your student alias to avoid deployment conflicts 
        },
        "postgreSQLServerName": {
            "value": "{alias}-dbsrv" //Replace {alias} with your student alias to avoid deployment conflicts 
        }
    }
}
